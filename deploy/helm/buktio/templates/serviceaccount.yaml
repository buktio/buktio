{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "buktio.serviceAccountName" . }}
  labels: {{- include "buktio.labels" . | nindent 4 }}
{{- end }}
