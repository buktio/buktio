# The embedded UI is generated by `make web-embed` (static export of apps/web).
# Only the placeholder is tracked so the Go package compiles without a web build;
# the real export (index.html, _next/, route folders) stays out of git.
/*
!/.gitignore
!/placeholder.html
